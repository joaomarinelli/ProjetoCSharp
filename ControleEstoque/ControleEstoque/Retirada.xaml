<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ControleEstoque"
        xmlns:Controllers="clr-namespace:Controllers;assembly=Controllers" x:Class="ControleEstoque.Retirada"
        mc:Ignorable="d"
        Title="Retirada" Height="300" Width="300">
    <Grid>
        <ComboBox x:Name="cbo_armazens" HorizontalAlignment="Left" Margin="48,26,0,0" VerticalAlignment="Top" Width="149" ItemsSource="{Binding}"  SelectedValuePath="ArmazemId" DisplayMemberPath="Local" SelectedItem="{Binding ItemSelecionado}" SelectionChanged="cbo_armazens_SelectionChanged">
            
        </ComboBox>

    </Grid>
</Window>
